## Packages
framer-motion | Smooth animations for result reveal and list transitions
lucide-react | Icons for UI elements (already in base but explicit check)

## Notes
API endpoints defined in shared/routes.ts:
- POST /api/analyze
- GET /api/history
- DELETE /api/history
